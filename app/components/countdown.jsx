const React = require('react');

const Countdown = React.createClass({
  render: function () {
    return (
      <div>
        Countdown Component
      </div>
    );
  }
});

module.exports = Countdown;
